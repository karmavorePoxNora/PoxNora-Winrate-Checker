# PoxNora Tracker — v34 runtime deps
PySide6>=6.6
pyqtgraph>=0.13
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Build-only dependency (optional; the build scripts install this automatically)
# pyinstaller>=6.0

